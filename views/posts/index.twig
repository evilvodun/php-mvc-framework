{% extends 'templates/app.twig' %}

{% block content %}
    <h1>Posts</h1>

    {% for post in posts %}
        <div class="post">
            {{ post.title }}
        </div>
    {% endfor %}
    
    {{ posts.links | raw }}
{% endblock content %}
